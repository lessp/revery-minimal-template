(executable
    (name App)
    (package App)
    (preprocess (pps brisk-reconciler.ppx))
    (public_name App)
    (libraries Revery Revery.lwt tablecloth-native fetch-native-lwt console.lib) 
    (flags (-open Tablecloth)))
